[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "tsl-log"
version = "0.1.2"
description = "Temporal Segment Log (TSL)"
authors = [{ name = "Vishwanath M M" }]
readme = "README.md"
requires-python = ">=3.8"